<%= render 'layouts/pagination' %>

<div class="container page_header">
  <div class="text-center">
    <h1>States</h1>
    <p>Ranking US states by the number of 510k applicants (for ophthalmic medical devices) they produced</p> 
  </div>
</div>

<div class="container">
  <div class="graph">
    <%= column_chart [{name: "Number of Applicants from each state", data: @plotOne}], xtitle: "States", 
    ytitle: "Number of applicants" %> 
  </div>
  <div class="right">
    <h1>Description</h1>
    <p>The plot looks at the number of 510k applicants (within ophthalmic devices) from 1990-2015 and categorizes the results by state. The top 3 states were California, New York, and Missouri. </p>
  </div>
</div>

<%= render 'layouts/pagination' %>

<%= render 'layouts/footer' %>






<%#= line_chart [
  {name: @result[0][0], data: @result[0][1]},
  {name: @result[1][0], data: @result[1][1]},
], xtitle: "Time interval", ytitle: "Number of applications" %>
